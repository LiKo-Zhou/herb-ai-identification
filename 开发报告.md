# 中药饮片智能鉴定系统 - 开发报告

## 版本历史

| 版本 | 日期 | 主要内容 |
|------|------|----------|
| v1.0 | 2026-04 | 级联推理架构（二分类 + 度量学习） |
| v2.0 | 2026-05 | Energy Score 零开销异常检测 |
| v2.5 | 2026-05-18 | PatchCore 记忆库 + v3.0 分层防御 |
| **v3.0** | **2026-05-19** | **融合推理测试完成，发现分布偏移问题** |
| **v3.1-feedback** | **2026-05-21** | **全局 Energy + Top-2仲裁 + 概率记忆库即时学习 + 前端纠正按钮** |

---

## 一、当前架构概览（v3.1 全局 Energy One-Class）

```
输入图像 (224×224 RGB)
        │
        ▼
┌─────────────────────────────┐
│  ResNet50 Phase 3 ONNX      │  ← 129 类品种鉴定（97.17% 准确率）
│  (Freeze Backbone + FC 微调) │
└──────────────┬──────────────┘
               │
               ▼
        ┌─────────────┐
        │  全局 Energy  │  E = -T·log(Σexp(logits/T))
        │  阈值 21.5    │  ← p95 of all authentic energies
        └──────┬──────┘
               │
      ┌────────┴────────┐
      ▼                 ▼
 E ≤ 21.5           E > 21.5
 (正常)              (告警)
      │                 │
      ▼                 ▼
  返回品种+药典     返回品种+药典+⚠️真伪存疑
        │
        ▼
  Top-2 种子类仲裁（麦芽/谷芽/菟丝子/莱菔子/薏苡仁）
```

---

## 二、v3.0 测试关键发现（2026-05-19）

### 2.1 测试数据

| 品类 | authentic | fake | 数据来源 |
|------|:---------:|:----:|----------|
| 丹参 (danshen) | 16 | 3 | `data_authenticity/` |
| 人参 (renshen) | 15 | 4 | `data_authenticity/` |
| 党参 (dangshen) | 15 | 5 | `data_authenticity/` |
| 酸枣仁 (suanzaoren) | 6 | 10 | `data_authenticity/` |

### 2.2 测试结果

| 品类 | authentic 样本 | fake 样本 | authentic 误报率 | fake 检出率 | 核心问题 |
|------|:------------:|:---------:|:---------------:|:----------:|----------|
| 丹参 | 16 | 3 | **0%** (0/16) | **66.7%** (2/3) | 表现最佳，纹理差异显著 |
| 人参 | 15 | 4 | **6.7%** (1/15) | **25.0%** (1/4) | 表现中等，存在一定重叠 |
| 党参 | 15 | 5 | **6.7%** (1/15) | **20.0%** (1/5) | 表现中等 |
| 酸枣仁 | 6 | 10 | **16.7%** (1/6) | **0%** (0/10) | 样本数少，分布偏移严重 |

### 2.3 问题根因分析

**根本原因：分布偏移 (Distribution Shift)**

```
data_90 训练集          vs          data_authenticity 验证集
    ↓                                      ↓
 拍摄条件、光线、背景、切割方式 完全不同
    ↓                                      ↓
 主模型对验证集图片识别率极低（dangshen 仅 20%, danshen 50%）
    ↓                                      ↓
 Top-1 识别错误 → 用错品类阈值 → Energy/PatchCore 失效
    ↓                                      ↓
 即使识别正确，分布差异导致 authentic 样本的能量/距离天然偏高
```

**关键证据**：大量 authentic 样本被主模型识别为完全不相关品类：
- 丹参 authentic → 蒲公英、柴胡、金银花、天麻
- 人参 authentic → 麦芽、金银花、石膏
- 党参 authentic → 天麻、人参、丹参、麦冬

**结论**：v3.0 架构（Energy + PatchCore）在理论上有效，但前提是**主模型能正确识别输入品类**。当前分布偏移导致这一前提不成立。

---

## 三、相关方法论文献综述

### 3.1 分布偏移下的异常检测

> **Cao et al., "Anomaly Detection Under Distribution Shift", ICCV 2023**

系统验证了当测试数据与训练数据存在分布偏移时，各类异常检测方法的 AUROC **暴跌 30-40%**。RD4AD 对分布偏移最鲁棒，因其基于知识蒸馏的表征学习方式。

**启示**：backbone 冻结 + FC 微调可能比全量微调更抗分布偏移。

### 3.2 Energy Score 跨域失效

> **Liu et al., "Energy-based Out-of-distribution Detection", NeurIPS 2020**（基础方法）

> **"Your Data Is Not Perfect: Towards Cross-Domain Out-of-Distribution Detection in Class-Imbalanced Data", 2024**

Energy Score 的假设是**测试数据与训练数据同分布**。一旦分布偏移，ID 样本的能量值会系统性偏高，导致误报率飙升。该文提出 Cross-Domain OOD Detection 任务，指出三大差距：
1. **语义差距 (Semantic Gap)**
2. **域差距 (Domain Gap)**
3. **类别不平衡差距 (Class-Imbalance Gap)**

### 3.3 无源领域自适应 (SFDA) ⭐ 最匹配场景

> **Liang et al., "SHOT: Do We Really Need to Access the Source Data?", ICML 2020**

SFDA 奠基之作。核心思想：**冻结源域分类器，仅用目标域无标签数据，通过信息最大化 + 伪标签自训练完成适应**。

> **Zhang et al., "Rethinking the Role of Pre-trained Networks in Source-Free Domain Adaptation", ICCV 2023**

发现：**预训练网络（ImageNet）本身携带的通用特征可以极大缓解 SFDA 中的伪标签噪声问题**。我们的 ResNet50 正是 ImageNet 预训练，具备这一优势。

> **Hwang et al., "SF(DA)²: Source-free Domain Adaptation through the Lens of Data Augmentation", ICLR 2024**

通过数据增强图 + 谱聚类识别目标域类别分区，无需源域数据。

### 3.4 测试时自适应 (TTA)

> **Wang et al., "Tent: Fully Test-Time Adaptation by Entropy Minimization", ICLR 2021**

在推理阶段，仅用当前 batch 的无标签测试数据，通过最小化预测熵来更新 BatchNorm 统计量。无需源域数据，计算开销极小。

> **"Test-time Adaptation with Diffusion Models"**

发现分类器在分布偏移时往往保持较高的 **Top-K 准确率**，但 Top-1 准确率大幅下降。这启发了 **Top-K Energy** 改进思路。

### 3.5 文献方案对照表

| 当前问题 | 对应文献方向 | 可行性 | 实施成本 |
|---------|:----------|:------:|:-------:|
| 训练/验证集分布差异大 | SHOT (SFDA) / Tent (TTA) | ⭐⭐⭐ | 低 |
| Energy Score 跨域失效 | Cross-Domain OOD Detection | ⭐⭐ | 中 |
| 主模型 Top-1 识别率低 | Test-Time Prototype Shifting | ⭐⭐⭐ | 极低 |
| 无源数据可用 | SFDA 系列 (SHOT, AaD, NRC) | ⭐⭐⭐ | 中 |
| 细粒度 + 小样本 | MADA (Multi-scale Attention + DA) | ⭐⭐ | 高 |

---

## 四、v3.1 最优方案：数据对齐 + Freeze Backbone 微调

### 4.1 方案选择依据

对比三种可行路径：

| 方案 | 原理 | 预期效果 | 实施难度 | 部署影响 |
|------|------|---------|:-------:|:-------:|
| A. Tent TTA | 推理时更新 BN 统计量 | 中 | 低 | 每次推理需梯度计算，板端不友好 |
| B. SHOT SFDA | 无监督自训练 | 中高 | 中 | 需目标域无标签数据，伪标签噪声风险 |
| **C. Phase 3 微调** | **合并 authentic 样本，freeze backbone 训 FC** | **高** | **低** | **一次训练，永久生效，板端零开销** |

**选择方案 C 的理由**：
1. `data_authenticity` 的 authentic 样本**有标签**，可直接做监督微调
2. 样本量虽小（每类 15 张），但 freeze backbone 后只训 FC 层，参数少，不易过拟合
3. 一次微调后导出 ONNX，板端推理无任何额外开销
4. 比无监督 SFDA 更稳定、更可预测

### 4.2 实施步骤

1. **数据合并**：将 `data_authenticity` 的 authentic 样本合并到 `data_90` 训练集中
2. **Phase 3 训练**：加载当前最佳模型（97.30%），freeze backbone，只训练 FC 层（5-10 epochs，lr=1e-4）
3. **验证**：检查 4 个品类的 Top-1 识别率是否提升
4. **导出 ONNX**：导出微调后的模型
5. **重新校准 Energy Score**：基于微调后的模型重新计算各品类能量分布
6. **重建 PatchCore 记忆库**：基于微调后的模型重新提取局部特征
7. **v3.1 融合推理验证**：评估 fake 检出率和 authentic 误报率

### 4.3 风险与应对

| 风险 | 应对策略 |
|------|----------|
| authentic 样本过少导致过拟合 | 使用强数据增强（RandAugment），lr 设低，epoch 控制在 5-10 |
| 微调后主模型在 data_90 上性能下降 | 保留原模型做对比验证，如下降则改用其他方案 |
| fake 样本仍不足 | fake 检出主要靠 Energy Score + PatchCore，不直接参与训练 |

---

---

## 五、麦芽/谷芽混淆专项优化（2026-05-20 新增）

### 5.1 问题发现

在 v3.1 验证过程中发现，麦芽（maiya）与谷芽（guya）这对外形高度相似的种子类药材存在显著的细粒度混淆：

| 品类 | 验证样本数 | Top-1 准确率 | 主要混淆对象 |
|:---|:---:|:---:|:---|
| 麦芽 | 155 | 89.7% | 谷芽（9张）、小茴香（2张） |
| 谷芽 | 134 | 77.6% | 菟丝子（9张）、麦芽（9张）、莱菔子（6张）、薏苡仁（3张） |

### 5.2 历史微调实验验证（全部失败）

| 实验 | 策略 | 麦芽准确率 | 谷芽准确率 | 结论 |
|:---|:---|:---:|:---:|:---|
| 增强微调 v2 | 冻结Backbone + 增强数据 + 加权采样 | **53.9%** 🔴 | **47.3%** 🔴 | 灾难性倒退 |
| 实验 A | 解冻Layer3/4/FC + Focal Loss | **32.0%** 🔴 | **74.8%** 🟡 | 麦芽彻底崩坏 |
| 实验 E | DINO预训练 + 对比学习 + Focal | **18.1%** 🔴 | **85.1%** 🟢 | 极度不平衡 |

**结论**：原始模型（未微调）在麦芽/谷芽上反而是表现最好的。所有微调均因分布偏移和损失函数不平衡而失败。

### 5.3 Top-2 后处理仲裁策略（当前最优方案）

不修改模型权重，仅通过后处理逻辑对种子类药材进行仲裁：

**仲裁规则**：
1. 麦芽↔谷芽概率差 < 0.20 → 触发"疑似"警告
2. 谷芽被错分为菟丝子/薏苡仁/莱菔子，且 Top-2 为谷芽、conf < 0.85 → 触发警告

**验证效果**：

| 品类 | 原始Top-1 | 等效准确率 | 错分挽救率 | 正确样本误报 |
|:---|:---:|:---:|:---:|:---:|
| 麦芽 | 89.7% | **93.5%** | 37.5% (6/16) | 5.8% (8/139) |
| 谷芽 | 77.6% | **87.3%** | 43.3% (13/30) | **0.0%** (0/104) |

### 5.4 种子类混淆深度分析

| 错分对象 | 数量 | 顽固程度 | 仲裁挽救率 | 针对性建议 |
|:---|:---:|:---:|:---:|:---|
| 麦芽 ↔ 谷芽 | 18（双向） | 中 | 100% | 核心难点，需二分类头或更高分辨率 |
| 菟丝子 | 9 | 中 | 77.8% | 增加颜色多样性样本 |
| 莱菔子 | 6 | **最顽固** | **50%** | **急需专门对比样本** |
| 薏苡仁 | 3 | 低 | 100% | 无需额外处理，仲裁已兜底 |

---

## 六、概率记忆库即时学习系统

### 6.1 设计原理

利用 softmax 概率向量作为特征指纹：
- 同类别相似度：**1.0**
- 跨类别相似度：**~10⁻¹²**

用户纠正后保存概率指纹，后续高相似度输入自动覆盖模型输出。

### 6.2 验证结果

| 步骤 | 输入 | 输出 | 状态 |
|:---|:---|:---|:---:|
| 第1次推理 | 党参分布偏移图 | 北沙参 | ❌ 错误 |
| 提交反馈 | correct_label=dangshen | — | ✅ 已保存 |
| 第2次推理 | 同一张图 | **党参** | ✅ 自动修正 |

---

## 七、严重问题记录

### 7.1 训练数据质量缺陷

训练数据混入电商图片，带有文字水印。白芍、白术等类别的训练图带有明显文字标签，导致模型学到非药材特征。

### 7.2 类别缺失

白芷（baizhi）、当归（danggui）、黄连（huanglian）、黄芪（huangqi）、川芎（chuanxiong）、熟地黄（shudihuang）、桂枝（guizhi）不在 129 类训练集中。

### 7.3 分布偏移

data_90 训练集与实拍验证集拍摄条件完全不同，导致广泛错分。

---

## 八、模型文件清单

| 文件 | 板端路径 | 大小 | 说明 |
|------|---------|:----:|------|
| `herb_resnet50_phase3.onnx` | `/opt/herb_ai/models/` | 90.6 MB | 主模型（v3.1 部署版） |
| `classes_129.txt` | `/opt/herb_ai/models/` | ~3 KB | 129 类药材名称 |
| `herb_dictionary.json` | `/opt/herb_ai/models/` | ~52 KB | 电子药典（100% 完整） |
| `energy_thresholds_phase3.json` | `/opt/herb_ai/models/` | ~28 KB | 全局 Energy 阈值 |
| `app.py` | `/opt/herb_ai/` | — | 完整前端服务（含记忆库+纠正按钮） |
| `feedback_memory.json` | `/opt/herb_ai/` | — | 概率记忆库 |
| `feedback/` | `/opt/herb_ai/feedback/` | — | 用户反馈图片存储 |
| `seed_arbitration.py` | `/opt/herb_ai/` | — | 种子类仲裁模块 |
| `diagnose_maiya_guya/` | `D:\herb_ai_local\` | — | 混淆诊断数据与图表 |
| `charts_comprehensive/` | `D:\herb_ai_local\` | — | 20 张综合可视化图表 |
| `auto_retrain.py` | `D:\herb_ai_local\` | — | PC端自动增量训练脚本框架 |

---

**报告版本**: v3.1-feedback
**最后更新**: 2026-05-21
